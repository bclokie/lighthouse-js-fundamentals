let map = (x, y)

const moves = ['north', 'north', 'west', 'west', 'north', 'east','north'] 

finalPosition(moves) = {
  if 'north' , y++;
  for 'west' in moves; x--;
  for 'east' in moves; x++
}

console.log(finalPosition(moves))